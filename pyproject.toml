[project]
name = "bolena-simplified"
version = "0.1.0"
description = "Email push listener for incident status updates via IMAP IDLE"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "IMAPClient>=3.0.1",
]

[tool.uv]
package = false
