services:
  - type: web
    name: service-status-email-tracker
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: IMAP_HOST
        value: imap.gmail.com
